{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Zero-Cloud capability set for Focus Engine. Minimal permissions for Hello World bootstrap. Filesystem (tauri-plugin-fs) and scoped shell permissions added in subsequent milestones.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-execute",
    "shell:allow-spawn",
    "shell:allow-kill"
  ]
}
